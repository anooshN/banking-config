# audit-service — dev overrides
spring:
  data:
    cassandra:
      contact-points: localhost
      port: 9042
      username: cassandra
      password: cassandra
  kafka:
    bootstrap-servers: localhost:9092
    properties:
      schema.registry.url: http://localhost:8085

banking:
  jwt:
    secret: dev-secret-key-that-is-at-least-32-characters-long
